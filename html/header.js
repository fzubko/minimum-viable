export function header($scope){
	console.debug('header started');
	// good place for session management, maybe redirects for bad urls
	
	// maybe grab the pathname so you can tweak breadcrumbs
	//$scope.pathname = $rootScope.pathname;
	//$rootScope.on('change', 'pathname', (value) => {
	//	$scope.pathname = $rootScope.pathname;
	//});
}